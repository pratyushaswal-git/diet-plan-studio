"use client";

// Placeholder — the live react-pdf <PDFViewer> lands in Batch 4.
export function PdfPreview() {
  return (
    <div className="flex h-full items-center justify-center rounded-lg border border-dashed border-app-rule bg-app-surface text-center text-sm text-app-muted">
      <span className="px-6">Live PDF preview arrives with Batch 4.</span>
    </div>
  );
}
